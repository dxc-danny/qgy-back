# 安全基础规范

## 哈希算法

【强制】token哈希：HMAC512

【强制】密码哈希：sha256

【强制】验证码哈希：md5

## 权限校验

【强制】后端接口要增加操作权限和数据权限判断

## 参数校验

【强制】后端写接口要自己做参数校验，不要依赖前端，避免乱七八糟的数据写入数据库

## 安全原则

1. 永远不要相信前端校验，后端必须独立校验所有输入
2. 敏感操作需要同时校验操作权限和数据权限
3. 密码等敏感信息必须使用不可逆哈希存储
4. token和密码必须使用不同的哈希算法
